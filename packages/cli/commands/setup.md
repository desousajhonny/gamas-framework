# `gamas setup`

Placeholder para abrir ou preparar o fluxo conversacional `/setup`.

Na v1, o setup profundo deve acontecer com o Gamas Agent dentro do VS Code.

Este comando pode futuramente:

- verificar se o workspace existe;
- orientar o usuario;
- abrir instrucoes do agente;
- validar manifests minimos.
