# Workflows

Workflows descrevem sequencias operacionais.

Use:

- `managed/` para referencias a workflows gerenciados;
- `custom/` para workflows criados pelo cliente;
- `overrides/` para configuracoes locais e ajustes declarativos.
